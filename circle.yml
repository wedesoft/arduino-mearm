dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get install arduino-mk arduino-core google-mock screen

test:
  override:
    - cp calibration.hh.default calibration.hh
    - env PATH=/usr/share/arduino:$PATH make
    - make check
